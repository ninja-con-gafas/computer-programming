#!/usr/bin/env bash

# Syntax: COMMAND -OPTIONS SOURCE TARGET

# mkdir create directory

# ls                list directory contents
# -a, --all         files including the hidden files
# -A                files excluding the current and parent directory
# -i                inode number and the file name
# -l                long list
# -r                files in reverse order
# -R                directories and sub directories recursively
# -S                sort by file size
# -t                sort by last modified

# pwd print the full filename of the current working directory

# cd change the shell working directory
# /         root directory
# ~         home directory
# ..        parent directory
# -         last directory

# cp copy files and directories
# -i, --interactive                        prompt before overwrite (overrides a previous -n option)
# -l, --link                               hard link files instead of copying
# -R, -r, --recursive                      copy directories recursively
# -s, --symbolic-link                      make symbolic links instead of copying
# -t, --target-directory=DIRECTORY         copy all SOURCE arguments into DIRECTORY
# -v, --verbose                            explain what is being done

# mv move (rename) files
# -l, --link            hard link files instead of copying
# -v, --verbose         explain what is being done

# rmdir remove empty directories
# -p, --parents         remove  DIRECTORY and its ancestors
# -v, --verbose         explain what is being done

# rm remove files or directories
# -i                            prompt before every removal
# -I                            prompt once before removing more than three files, or when re‐moving recursively; less intrusive than -i
# --no-preserve-root            do not treat '/' specially
# --preserve-root[=all]         do  not  remove '/' (default)
# -r, -R, --recursive           remove directories and their contents recursively
# -d, --dir                     remove empty directories
# -v, --verbose                 explain what is being done
